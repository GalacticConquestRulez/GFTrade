# NEVER commit these — wallet.json is a plaintext private key
wallet.json
.env

# runtime state
state.json
onchain_state.json
factor_log.db
factor_log.db-*

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
